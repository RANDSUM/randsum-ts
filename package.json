{
  "name": "randsum",
  "description": "Random Numbers, free of charge",
  "license": "MIT",
  "author": "Alex Jarvis",
  "repository": {
    "type": "git",
    "url": "https://github.com/alxjrvs/randsum.git"
  },
  "version": "0.3.4",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc -p .",
    "test": "jest",
    "generate:docs": "typedoc --out docs src --exclude src/**/index.ts --exclude src/**/**/*.test.ts  --theme minimal",
    "test:debug": "node --inspect-brk node_modules/.bin/jest -o --runInBand",
    "lint": "eslint . --ext .ts -c .eslint",
    "lint:fix": "eslint . --ext .ts  -c .eslint --fix",
    "watch-build": "tsc -w -p .",
    "prepare": "husky install"
  },
  "files": [
    "build/**/*"
  ],
  "devDependencies": {
    "@types/jest": "^27.0.0",
    "@typescript-eslint/eslint-plugin": "^5.7.0",
    "@typescript-eslint/parser": "^5.7.0",
    "codecov": "^3.8.3",
    "eslint": "^8.5.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^7.0.4",
    "jest": "^27.0.0",
    "lint-staged": "^12.1.3",
    "prettier": "^2.5.1",
    "ts-jest": "^27.0.0",
    "typedoc": "^0.22.10",
    "typescript": "~4.5.4",
    "yarn": "^1.22.0"
  },
  "keywords": [
    "chance",
    "dice",
    "dice rolls",
    "rpg",
    "rpgs",
    "die",
    "die rolling",
    "rand",
    "rolling",
    "typescript"
  ],
  "engines": {
    "node": ">=6.2.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "./src/**": "eslint --cache -c .eslint --fix"
  },
  "jest": {
    "coverageDirectory": "./coverage/",
    "collectCoverage": true,
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "(/__tests__/.*|(\\.|/)(test|spec))\\.(js|ts)$",
    "moduleDirectories": [
      "node_modules",
      "src"
    ],
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "rootDir": "src/"
  }
}
